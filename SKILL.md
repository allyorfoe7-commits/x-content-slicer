---
name: x-content-slicer
description: 抓取或读取使用者自己的 X/Twitter 内容，筛选可独立阅读的原创主帖，保留原意进行内容切片与长文排版优化，并批量生成带可配置账号、背景、卡片大小、位置、主题和互动数据的 3:4/9:16 PNG 图片与 3 秒 Live MP4 视频。用于“整理我的 X”“从我的推文里选内容”“推文变图片”“图片变 Live 图”“批量做推文卡片”“调整点赞收藏浏览量”等任务；适用于任意账号，不绑定特定作者或文风。
---

# X 内容切片

把使用者自己的 X 内容变成可独立传播的图片卡片和 Live 视频。身份、内容、头像、背景、尺寸与数据全部来自本次任务，不沿用其他人的资料。

## 工作流

### 1. 确定内容所有者

- 将“我的 X”解释为当前使用者自己的账号，不把模板作者当作内容所有者。
- 已给推文 URL 时，读取该推文；已给账号时，读取该账号；只有“我的 X”时，优先使用现有登录浏览器定位本人主页。
- 不索要账号密码。批量读取可使用现有登录会话、用户提供的 X 数据归档或用户自行配置的官方 API。
- 不把 Cookie、Token、原始归档或私人头像提交到 Skill 仓库。

### 2. 获取并筛选素材

收集正文、日期、来源 URL、作者昵称、用户名、头像和可用的真实互动数据。批量抓取时遵循：

- 只选作者主动发布的原创主帖。
- 排除回复、纯转发，以及脱离被引用内容就无法理解的引用帖。
- 引用帖只有在作者自己的正文能独立成立时才保留。
- 为每条候选保留来源 URL，避免切片后失去出处。

需要判断长文如何保留时，读取 [references/content-rules.md](references/content-rules.md)。

### 3. 切片和优化正文

按以下顺序处理可读性，越靠后改动越大：

1. 保留原文，只增加合理分段和空行。
2. 调整卡片比例、字号和缩放。
3. 仍然看不清时才压缩正文。

压缩时保留开头语境、核心比喻或论证、关键转折和结论。不要只截中间一句，不新增事实，不把别人的经历写成作者经历。默认生成一张完整卡；确需拆分时，每张都要能独立读懂，并按 `1/N` 标注顺序。

### 4. 设定视觉和数据

默认使用：

- 3:4，1080×1440。
- 黑色卡片，高端写实背景。
- 背景优先红酒、雪茄、跑车、高尔夫、私人会所；避免普通旅游风景、Logo、水印和画面文字。
- 卡片缩放 `92`，位置 `x=-20`、`y=-37`，长文由渲染器自动二次适配。
- 输出 PNG 和 3 秒、30fps、H.264 MP4。

互动数据支持精确数值、`high` 和 `viral` 两档展示预设，也可以关闭。若使用展示预设，不把它描述成平台真实分析数据；渲染结果旁的 JSON 清单会记录 `metricsSource: "display"`。

完整参数见 [references/config.md](references/config.md)。

### 5. 创建配置并渲染

在任务工作目录创建 JSON 配置，不要把用户内容写进 Skill 安装目录。单条配置可为对象，批量配置可为数组。

首次运行若缺少依赖，在本 Skill 目录执行：

```bash
npm install --registry=https://registry.npmmirror.com
```

渲染：

```bash
node scripts/render.mjs --config /绝对路径/cards.json --output /绝对路径/成品目录
```

只输出一种格式：

```bash
node scripts/render.mjs --config /绝对路径/cards.json --output /绝对路径/成品目录 --only png
node scripts/render.mjs --config /绝对路径/cards.json --output /绝对路径/成品目录 --only live
```

脚本会短暂启动仅监听本机的临时页面，任务结束自动关闭，不会部署服务。

### 6. 检查和交付

- 逐张查看 PNG，确认正文没有裁切、头像昵称正确、背景不抢字、数字完整。
- 长文若字号仍小，优先重新切片，不要无限缩字。
- 确认 MP4 与 PNG 同版，时长、尺寸、帧率符合配置。
- 交付可点击的 PNG、MP4 和 `render-manifest.json` 路径；批量任务同时给出数量与失败项。

## 资源

- `scripts/render.mjs`：配置驱动的批量 PNG/Live 渲染器。
- `assets/studio/`：无账号数据的本地卡片模板、视频编码依赖与五套高端背景。
- `references/config.md`：配置字段、预设和批量示例。
- `references/content-rules.md`：原创内容筛选、长文优化与切片边界。
